import { DigimartValidators } from './digimart-validators';

describe('DigimartValidators', () => {
  it('should create an instance', () => {
    expect(new DigimartValidators()).toBeTruthy();
  });
});
